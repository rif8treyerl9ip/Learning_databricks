# This is a Databricks asset bundle definition for data_pipeline.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: data_pipeline
  uuid: e3db9432-2f90-40ec-9864-08997c20d0da

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-ff6c8424-0cd6.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-ff6c8424-0cd6.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/5saphone92@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/5saphone92@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: 5saphone92@gmail.com
        level: CAN_MANAGE
